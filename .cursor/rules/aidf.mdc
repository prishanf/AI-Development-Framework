---
description: AI Development Framework adapter rules
alwaysApply: true
---

Follow the adapter-neutral rules in `AGENTS.md`.

Read the matching prompt contract in `commands/` before acting in that role. These are documents, not executable commands — this repository ships no command surface.

Preserve human gates. Report evidence, and never assert a check result a runner did not produce.
