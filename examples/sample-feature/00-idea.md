# Idea: Saved searches

Frequent users repeat the same search filters every day. Let users save a named search and run it again later from their search dashboard.

Requested by: Customer support feedback

Initial concern: saved filters may expose records a user no longer has permission to view, so permissions must be evaluated at run time rather than only when saving.
